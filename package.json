{
  "name": "html-static-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-catalog.mjs && node scripts/generate-collections.mjs && node scripts/generate-sitemap.mjs && vite build && node scripts/generate-seo-pages.mjs && node scripts/apply-consent.mjs",
    "preview": "vite preview",
    "generate:catalog": "node scripts/generate-catalog.mjs",
    "predev": "node scripts/generate-catalog.mjs && node scripts/generate-collections.mjs",
    "generate:collections": "node scripts/generate-collections.mjs"
  },
  "dependencies": {
    "lucide": "^0.469.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
